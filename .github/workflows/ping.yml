name: Ping every 15 minutes

on:
  workflow_dispatch:
  schedule:
    - cron: "*/15 * * * *"

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping MementoMori API
        run: curl -s -o /dev/null -w "%{http_code}" "${{ secrets.API_TOKEN }}")
